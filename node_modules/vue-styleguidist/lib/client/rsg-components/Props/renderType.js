import React from 'react';
import Type from 'rsg-components/Type';
import renderTypeString from '../../utils/renderTypeString';
export default function renderTypeBox(prop, classes) {
  return React.createElement(Type, null, React.createElement("pre", null, renderTypeString(prop.type), prop.required ? React.createElement("span", {
    className: classes.required
  }, " - required") : null));
}